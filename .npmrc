# pnpm — NestJS / reflect-metadata 호환 위해 hoisted node_modules 사용.
# 일부 라이브러리(passport, firebase-admin)가 hoisting 가정해 깨질 수 있어 strict 분리는 피함.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
