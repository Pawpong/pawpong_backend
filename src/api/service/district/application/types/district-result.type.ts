export type DistrictPublicResult = {
    city: string;
    districts: string[];
};

export type DistrictAdminResult = {
    id: string;
    city: string;
    districts: string[];
    createdAt: Date;
    updatedAt: Date;
};
