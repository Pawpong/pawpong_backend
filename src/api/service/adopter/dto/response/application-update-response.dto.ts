import { ApplicationCreateResponseDto } from './application-create-response.dto';

/**
 * 입양 신청서 수정 응답 DTO — 생성 응답과 필드는 같고 안내 메시지만 다르다.
 */
export class ApplicationUpdateResponseDto extends ApplicationCreateResponseDto {}
